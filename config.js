/* ============================================================
   ЕДИНАЯ КОНФИГУРАЦИЯ САЙТА
   Здесь меняются контакты и адрес приёма заявок. Больше нигде.
   ============================================================ */
window.SITE = {
  // Куда формы отправляют заявку. Это URL Яндекс Cloud Function, которая
  // шлёт письмо на вашу Яндекс.Почту (SMTP-пароль хранится в переменных
  // функции, в коде сайта его нет — безопасно и в РФ-контуре).
  // Пока функция не подключена — оставьте как есть: форма мягко скажет
  // «позвоните», данные никуда не утекут.
  leadEndpoint: 'https://REPLACE_WITH_YANDEX_FUNCTION',

  phone: '+7 (952) 101-93-00',
  phoneHref: 'tel:+79521019300',
  whatsapp: 'https://wa.me/79521019300',
  telegram: 'https://t.me/vskgarant',
  // MAX (мессенджер). Заглушка: временно ведёт на max.ru. Когда будет профиль —
  // заменить на ссылку из приложения (аватар → QR → Поделиться → max.ru/u/XXXX).
  max: 'https://max.ru/',

  // Регион в текстах
  region: 'Воронеж и Воронежская область',

  // Номер счётчика Яндекс.Метрики. 0 = не подключена (Метрика не грузится).
  // Впишите номер счётчика — Метрика включится после согласия на cookie.
  metrikaId: 0,
};
